{
  "name": "untitled1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "vite": "^7.1.9"
  },
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server \"npm --prefix client run dev\" \"npm --prefix server run dev\""
  },
  "devDependencies": {
    "concurrently": "^9"
  }
}
